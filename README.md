# AMP'D 12V

Brand-neutral Australian 12V system sizing calculator built with React, Vite and Tailwind CSS.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

Vercel should detect Vite automatically. Build command: `npm run build`. Output directory: `dist`.
